export interface MovieInfo {
    id: number,
    title: string,
    image: string,
    director: string,
    genres: string, 
    duration: number, 
    score: number, 
    rating: number, 
    overview: string, 
    year: number, 
    actors: string 
}